# Claude Code Instructions

Repository-wide agent policy lives in `AGENTS.md`. Read and follow it before any work.

@AGENTS.md

Claude-specific MCP servers are mirrored from `.codex/config.toml` in `.mcp.json`.
